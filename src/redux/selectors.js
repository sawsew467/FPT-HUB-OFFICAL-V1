export const darkmodeSelector = (state) => state.darkmode;
export const postsSelector = (state) => state.postList;
export const usersSelector = (state) => state.userList;
export const moblieMenuSelector = (state) => state.moblieMenu;

// export default darkmodeSelector;